# 登录校验（仅服务端，勿用 NEXT_PUBLIC_）
AUTH_USERNAME=admin
AUTH_PASSWORD=admin123

# 后端基址（BFF / SSR 直连）
BACKEND_URL=http://localhost:8080
# 刷新 JWT：POST body { "refreshToken" }，响应 data 含 accessToken / refreshToken / expiresIn / refreshIn
BACKEND_REFRESH_PATH=/refresh
# 拉用户信息：POST body { "userId" }，需 X-Access-Token（BFF 缺 userId 时从 JWT 解析）
BACKEND_USER_PATH=/api/get-user
